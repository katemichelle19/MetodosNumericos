####Java

```java
package com.mycompany.secante1;

import java.util.Scanner;
import java.lang.Math;

//Calcular la media de las secantes de tres ángulos ingresados por el usuario.
public class Secante4 {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Ingrese el primer ángulo en grados: ");
        double angle1 = scanner.nextDouble();
        double secant1 = 1 / Math.cos(Math.toRadians(angle1));

        System.out.print("Ingrese el segundo ángulo en grados: ");
        double angle2 = scanner.nextDouble();
        double secant2 = 1 / Math.cos(Math.toRadians(angle2));

        System.out.print("Ingrese el tercer ángulo en grados: ");
        double angle3 = scanner.nextDouble();
        double secant3 = 1 / Math.cos(Math.toRadians(angle3));

        double averageSecants = (secant1 + secant2 + secant3) / 3;
        System.out.println("La media de las secantes es: " + averageSecants);
    }
}


```



###FIN :)
